# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration (optional — uses local fallback if not set)
MONGODB_URI=mongodb://localhost:27017/parallax-portfolio

# Email Configuration (optional — emails disabled if not configured)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
ADMIN_EMAIL=admin@example.com

# Admin Authentication (for /admin page and admin endpoints)
ADMIN_KEY=your-secret-admin-key-change-this
